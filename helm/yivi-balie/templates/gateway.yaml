apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: yivi
spec:
  secretName: yivi-gateway-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - "yivi-server.prod.brainhive.nl"

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: yivi-gateway
spec:
  gatewayClassName: envoy
  listeners:
  - name: http
    port: 80
    protocol: HTTP
  - name: https
    port: 443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: tls-cert-irma-server
      - kind: Secret
        name: tls-cert-balie-server
